# Issue #9 — Task 2: Player screen + `play/<sessionId>` routing

## Branch/base

Branch `v09-09-dm-player-visibility`, based on `origin/main` at `98ad3722f3cc2c8fcaf4b6337d41dd07aada4579`. Verify with `git fetch origin && git rev-parse origin/main` before starting.

**Dependencies:** Task 1 (`src/board/displayView.js`, `src/board/boardViewRenderer.js`) and Task 3 (`src/screens/dm-screen.js`) must already exist, pass their own tests, and be committed before you start. You import both `createDmScreen` (Task 3) and, indirectly through your own new module, Task 1's exports. If either is missing, stop and report rather than reimplementing it yourself — reimplementing another task's owned file is exactly the duplicate-ownership problem this plan's task boundaries exist to prevent.

Read first, do not modify (beyond what's listed under "Owned files"):
- `docs/superpowers/plans/2026-09-24-issue-09-dm-player-visibility.md` — full plan; this is Task 2. Read Locked Rules D and E carefully — they are what your routing changes are actually accountable to.
- `src/screens/dm-screen.js` and its tests — the exact interface you wire in; read the real implementation, not just the plan sketch.
- `src/entry/app.js` in full (308 lines) — this is the file you modify. Understand `ensureRealtimeLifecycle()`, `startRealtimeBoard()`, `stopRealtimeBoard()`, the `load()` route dispatch (especially the DM-only redirect at line ~134 and the existing `board` branch at line ~159), and the four places `stopRealtimeBoard()`/`clearCharacters()` are already called (top of `load()`, `logout` action, the external auth-state-change handler, `pagehide`) — your changes must be called from exactly the same set of places, following the same convention, not a new one.
- `src/entry/service.js` — confirm `api.lobby(sessionId)` takes a session id and returns `{ status, ... }`; the existing `lobby/<id>` route (line ~171) and the `refresh()` function (line ~195) already use it this way — you are reusing that exact call, not inventing a new API.
- `tests/entry-ui.test.js` — the existing jsdom+fake-Supabase-client test harness for `startEntry`. Read how 8C.1 added its three board-route teardown tests (mentioned in `docs/testing/issue-08c-verification.md`'s "8C.1" section) — your new tests for the `play` route follow the identical pattern.
- `tests/e2e/realtime-harness.js`'s `expectGuestBoardIsolated(actor)` — the exact assertion (`#legacyBoard` hidden, zero `.cell` elements) your new route must never violate. You do not edit this file, but your route logic is directly accountable to it.

## Owned files

- `src/screens/player-screen.js` (new)
- `tests/player-screen.test.js` (new)
- `src/entry/app.js` (modify)
- `style.css` — additive: append the shared `.realtime-session-panel`, `.player-board-panel`, and the base `.realtime-token-list`/`.realtime-character-card`/`.realtime-initiative-list`/`.realtime-dm-section`/`.realtime-action-button` rules used by both the new player panel and Task 3's DM preview panel. Do not remove or reorder anything existing.
- `tests/entry-ui.test.js` — additive tests only (see below); do not alter or remove any existing test.

## Forbidden files

`script.js` (zero diff, no exceptions — see the plan's "one deliberate deviation" note for why), `src/screens/dm-screen.js` (Task 3's file — you import from it, never edit it), anything under `supabase/**`, `tests/e2e/**` (Task 4's responsibility).

## Allowed interface you implement

```js
// src/screens/player-screen.js
import { deriveDisplayView } from '../board/displayView.js';
import { renderBoardView } from '../board/boardViewRenderer.js';

/**
 * @param {HTMLElement} root
 * @returns {{ render(view: object|null): void, dispose(): void }}
 */
export function mountPlayerScreen(root) { }
```
Contract:
- Creates one `<section id="playerBoardPanel" class="realtime-session-panel player-board-panel" aria-label="Your session board">` appended to `root`, hidden by default.
- `render(view)` calls `renderBoardView(panel, deriveDisplayView(view, 'player'), { presentationMode: 'player' })`.
- `dispose()` removes the panel from the DOM; idempotent (safe to call twice). `render()` after `dispose()` is a no-op (does not throw, does not recreate the panel).

## Exact `src/entry/app.js` changes

1. Add imports: `import { mountPlayerScreen } from '../screens/player-screen.js';` and `import { createDmScreen } from '../screens/dm-screen.js';`.
2. Create the DM screen once, next to where `back` is created (near line 73–76): `const dmScreen = createDmScreen({ apply: view => window.aureRelicsApplyRealtimeSnapshot?.(view), container: document.querySelector('.app-header') });`.
3. Add state: `let renderTarget = null; let playerScreen = null;` and `function ensurePlayerScreen() { return playerScreen ??= mountPlayerScreen(root); }`.
4. Add a dispatcher: `function renderCurrent(view) { if (renderTarget === 'player') ensurePlayerScreen().render(view); else if (renderTarget === 'dm') dmScreen.render(view); }`.
5. Inside `ensureRealtimeLifecycle()`, replace the body of `onSnapshot` (currently `realtimeBoardView = reconcileBoardView(realtimeBoardView, snapshot); window.aureRelicsApplyRealtimeSnapshot?.(realtimeBoardView);`) with `realtimeBoardView = reconcileBoardView(realtimeBoardView, snapshot); renderCurrent(realtimeBoardView);`. Replace the body of `onStatus`'s `denied` branch similarly: `realtimeBoardView = null; renderCurrent(null);` in place of the direct `window.aureRelicsApplyRealtimeSnapshot?.(null)` call.
6. In `stopRealtimeBoard()`: call `renderCurrent(null);` before nulling `realtimeBoardView`/`realtimeSessionId`; then `playerScreen?.dispose(); playerScreen = null;` and `dmScreen.deactivate(); renderTarget = null;`. Keep the existing `document.getElementById('realtimeMutationNotice')?.remove();` line as-is.
7. In the existing `page === 'board'` branch (inside the `['session', 'board'].includes(page) && isDm(user)` block): immediately before the existing `startRealtimeBoard(session.id);` call, add `dmScreen.activate(); renderTarget = 'dm';`.
8. Add a new branch, after the existing `page === 'lobby'` branch and before the final `go(isDm(user) ? 'dm' : 'join');` fallback:
   ```js
   if (page === 'play' && user) {
     const lobby = await api.lobby(id);
     if (stamp !== epoch) return;
     if (lobby?.status !== 'approved') { go(`lobby/${id}`); return; }
     activeSession = { id };
     root.hidden = true; back.hidden = false;
     document.body.classList.remove('entry-active');
     renderTarget = 'player';
     startRealtimeBoard(id);
     return;
   }
   ```
   **Critical, non-negotiable constraint**: this branch must never reference `board`/`bootBoard`/`boardStarted`, and must never set `board.hidden = false`. `#legacyBoard` stays exactly as `concealBoard()` (called at the top of every `load()`) already left it: hidden. This is what makes Locked Rule E true by construction rather than by convention.
9. Double-check the earlier DM-only redirect at line ~134 (`if (['dm', 'campaign', 'session', 'board'].includes(page) && !isDm(user)) { ... }`) — do **not** add `'play'` to that array; `play` must remain reachable by non-DM users. Confirm this by reading the surrounding logic, not by assumption.

## Tests required (write first; must fail before implementation, pass after)

`tests/player-screen.test.js` (jsdom, mirrors Task 1's/Task 3's test style):
1. `mountPlayerScreen(root)` appends exactly one panel to `root`, hidden by default.
2. `render(view)` with a DM-shaped view (`dm` populated, `authority.canManage: true`) produces zero `.realtime-dm-section` and zero manage-only buttons (same defense-in-depth case as Tasks 1 and 3).
3. `render(null)` hides and clears the panel.
4. `dispose()` removes the panel; calling it twice does not throw; `render()` after `dispose()` does not throw and does not recreate the panel.

New additive tests in `tests/entry-ui.test.js` (follow the existing fake-Supabase-client/jsdom harness already used by every other test in that file):
1. An approved player navigating to `#play/<sessionId>` sees `#playerBoardPanel` become visible with rendered content, once a fake snapshot is delivered through the fake Supabase client's `get_session_snapshot`/channel stubs (reuse the same fake-client extension 8C.1 added — a `channel()`/`removeChannel()` stub plus a `get_session_snapshot` RPC branch).
2. A player whose fake lobby status is `pending` (or `revoked`, or missing) navigating to `#play/<sessionId>` is redirected to `#lobby/<sessionId>` and `#playerBoardPanel` is never created.
3. Entering `#play/<sessionId>` never calls the injected `bootBoard` spy (assert its call count is unchanged from before navigation) and never sets the `#legacyBoard` element's `hidden` attribute to `false`.
4. Leaving the `play` route (navigating to another hash) disposes `#playerBoardPanel` and tears down the real subscription — assert via the fake Supabase client's `channel()`/`removeChannel()` call counts, the same technique 8C.1 already used for the `board` route's own teardown test.
5. A stale-approved-then-backend-denies scenario: the fake `get_session_snapshot` RPC returns a `42501` error on the second call (simulating revocation mid-session); assert `#playerBoardPanel` becomes hidden/empty and no previously-rendered token/character content survives in its `innerHTML`.

## Completion report format

1. Files created/modified, with a one-line description of each `app.js` change mapped to the numbered steps above.
2. Full diff of `src/entry/app.js` (or a summary if the diff is large) confirming no change outside the 9 numbered steps above.
3. Confirmation `script.js` has zero diff (`git diff --stat script.js` produces no output).
4. Test output: `node --test tests/player-screen.test.js tests/entry-ui.test.js`, then `npm.cmd run check`, then `npm.cmd run build`.
5. Any deviation from the exact code shown above, with rationale.

## STOP condition

Stop and report once:
- `tests/player-screen.test.js` and the new `tests/entry-ui.test.js` cases all pass, and every pre-existing `tests/entry-ui.test.js` case still passes unmodified.
- `npm run check` and `npm run build` both pass.
- `script.js` diff is empty.
- Exactly one commit made: `feat: add player screen and play/<sessionId> route`.

Do not start Task 4's Playwright work or Issue #10. Do not modify `src/screens/dm-screen.js` even if you notice something you'd change — report it instead.
